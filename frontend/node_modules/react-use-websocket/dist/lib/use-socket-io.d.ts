import { Options, WebSocketHook } from './types';
export interface SocketIOMessageData {
    type: string;
    payload: any;
}
export declare const useSocketIO: (url: string | (() => string | Promise<string>) | null, options?: Options, connect?: boolean) => WebSocketHook<SocketIOMessageData>;
